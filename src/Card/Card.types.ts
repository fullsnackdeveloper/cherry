
export interface CardProps {
    title?: string,
    category?: string,
    image?: string,
    type?: 'post' | 'category',
    size?: 'small' | 'large' | 'list',
    icon?: string,
    iconMessage?: string,
    author?: string,
}
